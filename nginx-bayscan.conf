# BayScan Nginx Configuration
# Copy this to: /etc/nginx/sites-available/bayscan

server {
    listen 80;
    server_name bayscan.app www.bayscan.app;

    # Redirect to HTTPS (will be enabled after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
    }

    location /static {
        proxy_pass http://127.0.0.1:8080/static;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location /api {
        proxy_pass http://127.0.0.1:8080/api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# HTTPS configuration (uncomment after SSL certificate is obtained)
# server {
#     listen 443 ssl http2;
#     server_name bayscan.app www.bayscan.app;
#
#     ssl_certificate /etc/letsencrypt/live/bayscan.app/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/bayscan.app/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /static {
#         proxy_pass http://127.0.0.1:8080/static;
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
# }
